var chai = require( '../../node_modules/chai/chai.js' );
var assert = chai.assert;
var expect = chai.expect;

describe( '', function( ) { // A standard describe block
  before( function( ) {
    // This happens before all tests in this describe
  });
  after( function( ) {
    // This happens after all tests in this describe
  });
  beforeEach( function( ) {
    // This happens before each test in this describe
  });
  afterEach( function( ) {
    // This happens after each test in this describe
  });
  it( '', function( ) {
    // Here's where the assertions go.
  });
});